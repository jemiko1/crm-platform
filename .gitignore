# Dependencies
node_modules/
backend/node_modules/
frontend/crm-frontend/node_modules/

# Environment variables
.env
.env.local
.env*.local
backend/.env
backend/crm-backend/.env
frontend/crm-frontend/.env.local
core-sync-bridge/.env
ami-bridge/.env
bridge-monitor/.env

# Build outputs
dist/
build/
.next/
out/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Database
*.db
*.sqlite

# Backups (optional - you might want to commit these)
# C:\CRM-Backups/

# Temporary files
tmpclaude-*/
**/tmpclaude-*
*.tmp

# Cursor IDE
.cursor/

# Electron release builds (use GitHub Releases instead)
crm-phone/release-*/

# Design assets (not source code)
CRM28 Logo Pack/

# Deploy artifacts
core-sync-bridge-deploy.zip
vm-deploy.ps1

# Audit reports (auto-generated)
.audit-reports/

# Test coverage
coverage/

# Patch files
*.patch

# Local offline git backups (do not commit)
backups/*.bundle

# Local / generated audio
*.wav

# Bug report video uploads
uploads/

# Screenshots and temp images
bridge-monitor-*.png

# Playwright MCP
.playwright-mcp/

# Local MCP config (may contain paths or secrets)
.mcp.json

# Local audit / analysis dumps (not source)
AUDIT_REPORT.md
SECURITY_MAP.md
DATABASE_INTEGRITY.md
FRONTEND_CONSISTENCY.md
BUSINESS_LOGIC_TRACE.md
PERFORMANCE_PROFILE.md
DEPENDENCY_HEALTH.md
TEST_PLANS.md
ENV_DIFF_REPORT.md

# Ad-hoc debugging artifacts (SQL snippets, JWT mint scripts, etc.)
# Reviewer in the call-logs-cache fix flagged these leaking into
# commits as a risk — one such file contained prod admin UUIDs +
# path to the JWT secret reader.
tmp-*
/tmp/
# Claude Code agent worktrees (never commit)
.claude/worktrees/
.worktrees/
