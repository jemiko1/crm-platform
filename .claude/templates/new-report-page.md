For statistics/report pages:
1. Backend: GET /v1/{module}/statistics — date range params, groupBy aggregations
2. Frontend: KPI cards + Recharts charts + date picker + loading skeletons
3. Wrap charts in <ResponsiveContainer width="100%" height={300}>
