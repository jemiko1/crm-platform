For admin config pages:
1. Backend: /v1/admin/{feature} — CRUD with {feature}_config.access permission
2. Frontend: /app/admin/{feature}/page.tsx — table with inline or modal editing
3. Sidebar menu item with permission gate
